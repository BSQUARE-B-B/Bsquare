# Netlify build configuration for Next.js (CurveClear)
# Netlify auto-detects Next.js and uses the OpenNext adapter — no plugin required.

[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js 15: ensure compatible Node
# Uncomment and set in Netlify UI if you use Supabase or other secrets:
# SUPABASE_URL = "..."
# SUPABASE_ANON_KEY = "..."
